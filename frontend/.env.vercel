# Vercel Deployment Configuration
# Copy this to your Vercel environment variables

# Data Mode: Use "sample" for Vercel deployment (no backend required)
NEXT_PUBLIC_DATA_MODE=sample

# Backend API URLs (not used in sample mode)
# NEXT_PUBLIC_API_URL=https://your-backend-api.com
# NEXT_PUBLIC_WS_URL=wss://your-backend-api.com/ws

# Mapbox (optional)
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here
